export const getRubrics = async (req, res) => {
  res.json({ message: "Get rubrics - faculty only" });
};

export const createRubric = async (req, res) => {
  res.json({ message: "Create rubric - faculty only" });
};

export const updateRubric = async (req, res) => {
  res.json({ message: "Update rubric - faculty only" });
};

export const deleteRubric = async (req, res) => {
  res.json({ message: "Delete rubric - faculty only" });
};
